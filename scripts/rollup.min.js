import banner from './banner';
import babel from 'rollup-plugin-babel';
import {terser} from "rollup-plugin-terser";


export default {
    input: 'src/Proxy.js',
    output: {
        file: 'dist/proxy-polyfill.min.js',
        format: 'umd',
        name: 'Proxy',
        banner,
        sourcemap: true,
    },
    plugins: [
        babel({
            presets: [
                ['@babel/preset-env', {
                    loose: true,
                }]
            ],
        }),
        terser({
            ie8: true,
        }),
    ],
};